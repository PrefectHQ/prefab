---
title: DataTableColumn
description: JSON Schema for the DataTableColumn component.
---

Column definition for DataTable.

```json
{
  "type": "object",
  "properties": {
    "key": {
      "description": "Data key to display in this column",
      "type": "string"
    },
    "header": {
      "description": "Column header text",
      "type": "string"
    },
    "sortable": {
      "default": false,
      "description": "Enable sorting for this column",
      "type": "boolean"
    },
    "format": {
      "type": [
        "string",
        "null"
      ],
      "default": null,
      "description": "Cell format: 'number', 'number:2' (decimals), 'currency', 'currency:EUR', 'percent', 'percent:1', 'date', 'date:long'"
    }
  },
  "required": [
    "key",
    "header"
  ]
}
```
