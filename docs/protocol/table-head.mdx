---
title: TableHead
description: JSON Schema for the TableHead component.
---

A header cell within a TableRow.

```json
{
  "type": "object",
  "properties": {
    "type": {
      "const": "TableHead",
      "default": "TableHead",
      "type": "string"
    },
    "cssClass": {
      "type": [
        "string",
        "null"
      ],
      "default": null,
      "description": "CSS/Tailwind classes for styling. Accepts a Responsive() for breakpoint-aware classes."
    },
    "children": {
      "type": "array",
      "items": {
        "$ref": "Component"
      }
    },
    "content": {
      "type": [
        "string",
        "null"
      ],
      "default": null,
      "description": "Header text (alternative to children)"
    }
  },
  "required": [
    "type"
  ]
}
```
