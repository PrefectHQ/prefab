---
title: Switch
description: Toggle switch for on/off states.
icon: toggle-on
---

import { ComponentPreview } from "/snippets/component-preview.mdx";

Switches provide a visual toggle for binary states, offering an alternative to checkboxes.

## Basic Usage

<ComponentPreview auto json={`{"view":{"type":"Switch","label":"Enable notifications","checked":false,"size":"default","disabled":false,"required":false}}`}>
<CodeGroup>
```python Python icon="python"
from prefab_ui.components import Switch

Switch(label="Enable notifications")
```
```json Protocol icon="brackets-curly"
{
  "view": {
    "type": "Switch",
    "label": "Enable notifications",
    "checked": false,
    "size": "default",
    "disabled": false,
    "required": false
  }
}
```
</CodeGroup>
</ComponentPreview>

## Checked State

<ComponentPreview auto json={`{"view":{"type":"Switch","label":"Dark mode","checked":true,"size":"default","disabled":false,"required":false}}`}>
<CodeGroup>
```python Python icon="python"
from prefab_ui.components import Switch

Switch(label="Dark mode", checked=True)
```
```json Protocol icon="brackets-curly"
{
  "view": {
    "type": "Switch",
    "label": "Dark mode",
    "checked": true,
    "size": "default",
    "disabled": false,
    "required": false
  }
}
```
</CodeGroup>
</ComponentPreview>

## Sizes

Switches come in two sizes:

<ComponentPreview auto json={`{"view":{"cssClass":"gap-4","type":"Column","children":[{"type":"Switch","label":"Default size","checked":true,"size":"default","disabled":false,"required":false},{"type":"Switch","label":"Small size","checked":true,"size":"sm","disabled":false,"required":false}]}}`}>
<CodeGroup>
```python Python icon="python"
from prefab_ui.components import (
    Column,
    Switch,
)

with Column(gap=4):
    Switch(label="Default size", checked=True)
    Switch(label="Small size", size="sm", checked=True)
```
```json Protocol icon="brackets-curly"
{
  "view": {
    "cssClass": "gap-4",
    "type": "Column",
    "children": [
      {
        "type": "Switch",
        "label": "Default size",
        "checked": true,
        "size": "default",
        "disabled": false,
        "required": false
      },
      {
        "type": "Switch",
        "label": "Small size",
        "checked": true,
        "size": "sm",
        "disabled": false,
        "required": false
      }
    ]
  }
}
```
</CodeGroup>
</ComponentPreview>

## Disabled State

<ComponentPreview auto json={`{"view":{"cssClass":"gap-3","type":"Column","children":[{"type":"Switch","label":"Enabled switch","checked":false,"size":"default","disabled":false,"required":false},{"type":"Switch","label":"Disabled off","checked":false,"size":"default","disabled":true,"required":false},{"type":"Switch","label":"Disabled on","checked":true,"size":"default","disabled":true,"required":false}]}}`}>
<CodeGroup>
```python Python icon="python"
from prefab_ui.components import (
    Column,
    Switch,
)

with Column(gap=3):
    Switch(label="Enabled switch")
    Switch(label="Disabled off", disabled=True)
    Switch(label="Disabled on", checked=True, disabled=True)
```
```json Protocol icon="brackets-curly"
{
  "view": {
    "cssClass": "gap-3",
    "type": "Column",
    "children": [
      {
        "type": "Switch",
        "label": "Enabled switch",
        "checked": false,
        "size": "default",
        "disabled": false,
        "required": false
      },
      {
        "type": "Switch",
        "label": "Disabled off",
        "checked": false,
        "size": "default",
        "disabled": true,
        "required": false
      },
      {
        "type": "Switch",
        "label": "Disabled on",
        "checked": true,
        "size": "default",
        "disabled": true,
        "required": false
      }
    ]
  }
}
```
</CodeGroup>
</ComponentPreview>

## API Reference

<Card icon="code" title="Switch Parameters">

<ParamField body="label" type="str | None" default="None">
  Label text displayed next to the switch.
</ParamField>

<ParamField body="checked" type="bool" default="False">
  Whether the switch is on.
</ParamField>

<ParamField body="size" type="str" default="default">
  Switch size: `"default"`, `"sm"`.
</ParamField>

<ParamField body="name" type="str | None" default="None">
  Form field name for submission.
</ParamField>

<ParamField body="disabled" type="bool" default="False">
  Whether the switch is non-interactive.
</ParamField>

<ParamField body="required" type="bool" default="False">
  Whether a selection is required for form submission.
</ParamField>

<ParamField body="css_class" type="str | None" default="None">
  Additional Tailwind CSS classes appended to the component's built-in styles.
</ParamField>
</Card>

## Protocol Reference

<Card icon="braces" title="Switch Protocol">
<ParamField body="cssClass" type="string | null">
  CSS/Tailwind classes for styling
</ParamField>

<ParamField body="type" type="Switch" default="Switch">
  Component discriminator.
</ParamField>

<ParamField body="label" type="string | null">
  Label text
</ParamField>

<ParamField body="checked" type="boolean" default="false">
  Whether switch is on
</ParamField>

<ParamField body="size" type="string" default="default">
  Switch size (sm, default). One of: `"sm"`, `"default"`
</ParamField>

<ParamField body="name" type="string | null">
  Form field name
</ParamField>

<ParamField body="disabled" type="boolean" default="false">
  Whether switch is disabled
</ParamField>

<ParamField body="required" type="boolean" default="false">
  Whether switch is required
</ParamField>

<ParamField body="onChange" type="Action | Action[] | null">
  Action(s) to execute when toggled
</ParamField>

</Card>
