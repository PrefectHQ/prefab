---
title: RadialChart
description: JSON Schema for the RadialChart component.
---

Radial bar chart — categorical data as concentric rings.

```json
{
  "type": "object",
  "properties": {
    "type": {
      "const": "RadialChart",
      "default": "RadialChart",
      "type": "string"
    },
    "cssClass": {
      "type": [
        "string",
        "null"
      ],
      "default": null,
      "description": "CSS/Tailwind classes for styling. Accepts a Responsive() for breakpoint-aware classes."
    },
    "data": {
      "anyOf": [
        {
          "items": {
            "additionalProperties": true,
            "type": "object"
          },
          "type": "array"
        },
        {
          "type": "string"
        }
      ],
      "description": "Row data or {{ interpolation }} reference"
    },
    "dataKey": {
      "description": "Numeric value field",
      "type": "string"
    },
    "nameKey": {
      "description": "Label field",
      "type": "string"
    },
    "height": {
      "default": 300,
      "description": "Chart height in pixels",
      "type": "integer"
    },
    "innerRadius": {
      "default": 30,
      "description": "Inner radius in pixels",
      "type": "integer"
    },
    "startAngle": {
      "default": 180,
      "description": "Arc start angle in degrees",
      "type": "integer"
    },
    "endAngle": {
      "default": 0,
      "description": "Arc end angle in degrees",
      "type": "integer"
    },
    "showLegend": {
      "default": false,
      "description": "Show legend",
      "type": "boolean"
    },
    "showTooltip": {
      "default": true,
      "description": "Show tooltip on hover",
      "type": "boolean"
    }
  },
  "required": [
    "type",
    "data",
    "dataKey",
    "nameKey"
  ]
}
```
