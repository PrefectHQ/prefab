---
title: Heading
description: JSON Schema for the Heading component.
---

Section heading (h1-h4).

```json
{
  "type": "object",
  "properties": {
    "type": {
      "const": "Heading",
      "default": "Heading",
      "type": "string"
    },
    "cssClass": {
      "type": [
        "string",
        "null"
      ],
      "default": null,
      "description": "CSS/Tailwind classes for styling. Accepts a Responsive() for breakpoint-aware classes."
    },
    "content": {
      "description": "Text content",
      "type": "string"
    },
    "bold": {
      "type": [
        "boolean",
        "null"
      ],
      "default": null,
      "description": "Render text in bold"
    },
    "italic": {
      "type": [
        "boolean",
        "null"
      ],
      "default": null,
      "description": "Render text in italic"
    },
    "align": {
      "enum": [
        "left",
        "center",
        "right",
        "justify"
      ],
      "type": [
        "string",
        "null"
      ],
      "default": null
    },
    "level": {
      "default": 1,
      "description": "Heading level (1=h1, 4=h4)",
      "enum": [
        1,
        2,
        3,
        4
      ],
      "type": "integer"
    }
  },
  "required": [
    "type",
    "content"
  ]
}
```
