---
title: Checkbox
description: Checkbox inputs for binary or multiple selections.
icon: square-check
---

import { ComponentPreview } from "/snippets/component-preview.mdx";

## Usage

Checkboxes let users select one or more options from a set.

<ComponentPreview auto json={`{"cssClass":"w-fit mx-auto","type":"Checkbox","label":"Accept terms and conditions","checked":false,"disabled":false,"required":false}`}>
<CodeGroup>
```python Python
from prefab_ui.components import Checkbox

Checkbox(label="Accept terms and conditions", css_class="w-fit mx-auto")
```
```json Protocol
{
  "cssClass": "w-fit mx-auto",
  "type": "Checkbox",
  "label": "Accept terms and conditions",
  "checked": false,
  "disabled": false,
  "required": false
}
```
</CodeGroup>
</ComponentPreview>

## Checked State

<ComponentPreview auto json={`{"cssClass":"w-fit mx-auto","type":"Checkbox","label":"Subscribe to newsletter","checked":true,"disabled":false,"required":false}`}>
<CodeGroup>
```python Python
from prefab_ui.components import Checkbox

Checkbox(label="Subscribe to newsletter", checked=True, css_class="w-fit mx-auto")
```
```json Protocol
{
  "cssClass": "w-fit mx-auto",
  "type": "Checkbox",
  "label": "Subscribe to newsletter",
  "checked": true,
  "disabled": false,
  "required": false
}
```
</CodeGroup>
</ComponentPreview>

## Multiple Checkboxes

<ComponentPreview auto json={`{"cssClass":"w-fit mx-auto","type":"Column","gap":3,"children":[{"type":"Checkbox","label":"Email notifications","checked":true,"disabled":false,"required":false},{"type":"Checkbox","label":"SMS notifications","checked":false,"disabled":false,"required":false},{"type":"Checkbox","label":"Push notifications","checked":true,"disabled":false,"required":false}]}`}>
<CodeGroup>
```python Python
from prefab_ui.components import (
    Checkbox,
    Column,
)

with Column(gap=3, css_class="w-fit mx-auto"):
    Checkbox(label="Email notifications", checked=True)
    Checkbox(label="SMS notifications")
    Checkbox(label="Push notifications", checked=True)
```
```json Protocol
{
  "cssClass": "w-fit mx-auto",
  "type": "Column",
  "gap": 3,
  "children": [
    {
      "type": "Checkbox",
      "label": "Email notifications",
      "checked": true,
      "disabled": false,
      "required": false
    },
    {
      "type": "Checkbox",
      "label": "SMS notifications",
      "checked": false,
      "disabled": false,
      "required": false
    },
    {
      "type": "Checkbox",
      "label": "Push notifications",
      "checked": true,
      "disabled": false,
      "required": false
    }
  ]
}
```
</CodeGroup>
</ComponentPreview>

## With Disabled State

<ComponentPreview auto json={`{"cssClass":"w-fit mx-auto","type":"Column","gap":3,"children":[{"type":"Checkbox","label":"Enabled option","checked":false,"disabled":false,"required":false},{"type":"Checkbox","label":"Disabled option","checked":false,"disabled":true,"required":false},{"type":"Checkbox","label":"Disabled & checked","checked":true,"disabled":true,"required":false}]}`}>
<CodeGroup>
```python Python
from prefab_ui.components import (
    Checkbox,
    Column,
)

with Column(gap=3, css_class="w-fit mx-auto"):
    Checkbox(label="Enabled option")
    Checkbox(label="Disabled option", disabled=True)
    Checkbox(label="Disabled & checked", checked=True, disabled=True)
```
```json Protocol
{
  "cssClass": "w-fit mx-auto",
  "type": "Column",
  "gap": 3,
  "children": [
    {
      "type": "Checkbox",
      "label": "Enabled option",
      "checked": false,
      "disabled": false,
      "required": false
    },
    {
      "type": "Checkbox",
      "label": "Disabled option",
      "checked": false,
      "disabled": true,
      "required": false
    },
    {
      "type": "Checkbox",
      "label": "Disabled & checked",
      "checked": true,
      "disabled": true,
      "required": false
    }
  ]
}
```
</CodeGroup>
</ComponentPreview>

## API Reference

<Card icon="code" title="Checkbox Parameters">

<ParamField path="label" type="str | None">
  Label text displayed next to the checkbox. Supports `{{ field }}` interpolation.
</ParamField>

<ParamField path="checked" type="bool" default="False">
  Whether the checkbox is checked.
</ParamField>

<ParamField path="name" type="str | None">
  Form field name for submission.
</ParamField>

<ParamField path="value" type="str | None">
  Form value when checked. Supports `{{ field }}` interpolation.
</ParamField>

<ParamField path="disabled" type="bool" default="False">
  Whether the checkbox is disabled.
</ParamField>

<ParamField path="required" type="bool" default="False">
  Whether the checkbox is required.
</ParamField>

<ParamField path="css_class" type="str | None">
  Additional CSS classes to apply.
</ParamField>
</Card>

## Protocol Reference

<Card icon="braces" title="Checkbox Protocol">
<ParamField body="cssClass" type="string | null">
  CSS/Tailwind classes for styling
</ParamField>

<ParamField body="visibleWhen" type="string | null">
  State key — component renders only when this state value is truthy
</ParamField>

<ParamField body="type" type="Checkbox" default="Checkbox">
  Component discriminator.
</ParamField>

<ParamField body="label" type="string | null">
  Label text (supports {{ field }} interpolation)
</ParamField>

<ParamField body="checked" type="boolean" default="false">
  Whether checkbox is checked
</ParamField>

<ParamField body="name" type="string | null">
  Form field name
</ParamField>

<ParamField body="value" type="string | null">
  Form value (supports {{ field }} interpolation)
</ParamField>

<ParamField body="disabled" type="boolean" default="false">
  Whether checkbox is disabled
</ParamField>

<ParamField body="required" type="boolean" default="false">
  Whether checkbox is required
</ParamField>

<ParamField body="onChange" type="Action | Action[] | null">
  Action(s) to execute when checked state changes
</ParamField>

</Card>
