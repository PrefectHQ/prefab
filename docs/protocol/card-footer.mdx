---
title: CardFooter
description: JSON Schema for the CardFooter component.
---

Card footer section, typically for actions.

```json
{
  "type": "object",
  "properties": {
    "type": {
      "const": "CardFooter",
      "default": "CardFooter",
      "type": "string"
    },
    "cssClass": {
      "type": [
        "string",
        "null"
      ],
      "default": null,
      "description": "CSS/Tailwind classes for styling"
    },
    "children": {
      "type": "array",
      "items": {
        "$ref": "Component"
      }
    }
  },
  "required": [
    "type"
  ]
}
```
