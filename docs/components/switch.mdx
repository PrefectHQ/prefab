---
title: Switch
description: Toggle switch for on/off states.
icon: toggle-on
---

import { ComponentPreview } from "/snippets/component-preview.mdx";

Switches provide a visual toggle for binary states, offering an alternative to checkboxes.

## Basic Usage

<ComponentPreview json={{"view":{"type":"Switch","label":"Enable notifications","checked":false,"size":"default","name":"switch-5","disabled":false,"required":false}}}>
<CodeGroup>
```python Python icon="python"
from prefab_ui.components import Switch

Switch(label="Enable notifications")
```
```json Protocol icon="brackets-curly"
{
  "view": {
    "type": "Switch",
    "label": "Enable notifications",
    "checked": false,
    "size": "default",
    "name": "switch-5",
    "disabled": false,
    "required": false
  }
}
```
</CodeGroup>
</ComponentPreview>

## Checked State

<ComponentPreview json={{"view":{"type":"Switch","label":"Dark mode","checked":true,"size":"default","name":"switch-6","disabled":false,"required":false}}}>
<CodeGroup>
```python Python icon="python"
from prefab_ui.components import Switch

Switch(label="Dark mode", checked=True)
```
```json Protocol icon="brackets-curly"
{
  "view": {
    "type": "Switch",
    "label": "Dark mode",
    "checked": true,
    "size": "default",
    "name": "switch-6",
    "disabled": false,
    "required": false
  }
}
```
</CodeGroup>
</ComponentPreview>

## Sizes

Switches come in two sizes:

<ComponentPreview json={{"view":{"cssClass":"gap-4","type":"Column","children":[{"type":"Switch","label":"Default size","checked":true,"size":"default","name":"switch-7","disabled":false,"required":false},{"type":"Switch","label":"Small size","checked":true,"size":"sm","name":"switch-8","disabled":false,"required":false}]}}}>
<CodeGroup>
```python Python icon="python"
from prefab_ui.components import (
    Column,
    Switch,
)

with Column(gap=4):
    Switch(label="Default size", checked=True)
    Switch(label="Small size", size="sm", checked=True)
```
```json Protocol icon="brackets-curly"
{
  "view": {
    "cssClass": "gap-4",
    "type": "Column",
    "children": [
      {
        "type": "Switch",
        "label": "Default size",
        "checked": true,
        "size": "default",
        "name": "switch-7",
        "disabled": false,
        "required": false
      },
      {
        "type": "Switch",
        "label": "Small size",
        "checked": true,
        "size": "sm",
        "name": "switch-8",
        "disabled": false,
        "required": false
      }
    ]
  }
}
```
</CodeGroup>
</ComponentPreview>

## With Actions

The `on_change` parameter runs actions when the switch is toggled. The `$event` variable is `true` when switched on, `false` when off. Here, toggling the switch updates a separate status message:

<ComponentPreview json={{"view":{"cssClass":"gap-4","type":"Column","children":[{"type":"Switch","label":"Enable notifications","checked":false,"size":"default","name":"notifications","disabled":false,"required":false,"onChange":{"action":"setState","key":"status","value":"{{ $event ? 'Notifications enabled' : 'Notifications disabled' }}"}},{"cssClass":"text-muted-foreground","content":"{{ status | 'Toggle the switch' }}","type":"Text"}]}}}>
<CodeGroup>
```python Python icon="python"
from prefab_ui.components import Column, Switch, Text
from prefab_ui.actions import SetState

with Column(gap=4):
    Switch(
        label="Enable notifications",
        name="notifications",
        on_change=SetState(
            "status",
            "{{ $event ? 'Notifications enabled' : 'Notifications disabled' }}",
        ),
    )
    Text(
        "{{ status | 'Toggle the switch' }}",
        css_class="text-muted-foreground",
    )
```
```json Protocol icon="brackets-curly"
{
  "view": {
    "cssClass": "gap-4",
    "type": "Column",
    "children": [
      {
        "type": "Switch",
        "label": "Enable notifications",
        "checked": false,
        "size": "default",
        "name": "notifications",
        "disabled": false,
        "required": false,
        "onChange": {
          "action": "setState",
          "key": "status",
          "value": "{{ $event ? 'Notifications enabled' : 'Notifications disabled' }}"
        }
      },
      {
        "cssClass": "text-muted-foreground",
        "content": "{{ status | 'Toggle the switch' }}",
        "type": "Text"
      }
    ]
  }
}
```
</CodeGroup>
</ComponentPreview>

## Disabled State

<ComponentPreview json={{"view":{"cssClass":"gap-3","type":"Column","children":[{"type":"Switch","label":"Enabled switch","checked":false,"size":"default","name":"switch-9","disabled":false,"required":false},{"type":"Switch","label":"Disabled off","checked":false,"size":"default","name":"switch-10","disabled":true,"required":false},{"type":"Switch","label":"Disabled on","checked":true,"size":"default","name":"switch-11","disabled":true,"required":false}]}}}>
<CodeGroup>
```python Python icon="python"
from prefab_ui.components import (
    Column,
    Switch,
)

with Column(gap=3):
    Switch(label="Enabled switch")
    Switch(label="Disabled off", disabled=True)
    Switch(label="Disabled on", checked=True, disabled=True)
```
```json Protocol icon="brackets-curly"
{
  "view": {
    "cssClass": "gap-3",
    "type": "Column",
    "children": [
      {
        "type": "Switch",
        "label": "Enabled switch",
        "checked": false,
        "size": "default",
        "name": "switch-9",
        "disabled": false,
        "required": false
      },
      {
        "type": "Switch",
        "label": "Disabled off",
        "checked": false,
        "size": "default",
        "name": "switch-10",
        "disabled": true,
        "required": false
      },
      {
        "type": "Switch",
        "label": "Disabled on",
        "checked": true,
        "size": "default",
        "name": "switch-11",
        "disabled": true,
        "required": false
      }
    ]
  }
}
```
</CodeGroup>
</ComponentPreview>

## API Reference

<Card icon="code" title="Switch Parameters">

<ParamField body="label" type="str | None" default="None">
  Label text displayed next to the switch.
</ParamField>

<ParamField body="checked" type="bool" default="False">
  Whether the switch is on.
</ParamField>

<ParamField body="size" type="str" default="default">
  Switch size: `"default"`, `"sm"`.
</ParamField>

<ParamField body="name" type="str | None" default="None">
  State key for the switch's checked state. Auto-generated if not provided. Use `.rx` to reference the switch's boolean state in other components without specifying the name explicitly.
</ParamField>

<ParamField body="disabled" type="bool" default="False">
  Whether the switch is non-interactive.
</ParamField>

<ParamField body="required" type="bool" default="False">
  Whether a selection is required for form submission.
</ParamField>

<ParamField body="on_change" type="Action | list[Action] | None" default="None">
  Action(s) to execute when toggled. `$event` is `true` when on, `false` when off.
</ParamField>

<ParamField body="css_class" type="str | None" default="None">
  Additional Tailwind CSS classes appended to the component's built-in styles.
</ParamField>
</Card>

## Protocol Reference

```json Switch
{
  "type": "Switch",
  "label?": "string",
  "checked?": false,
  "size?": "sm | default",
  "name?": "string",
  "disabled?": false,
  "required?": false,
  "onChange?": "Action | Action[]",
  "cssClass?": "string"
}
```

For the complete protocol schema, see [Switch](/protocol/switch).
