export const ComponentPreview = ({ json, height = "120px" }) => (
  <div className="not-prose">
    <iframe
      data-docpreview
      src={`http://localhost:3333/renderer.html#docpreview:${encodeURIComponent(json)}`}
      style={{ width: "100%", height, border: "none", display: "block", borderRadius: "0.75rem" }}
    />
  </div>
);
