---
title: TableRow
description: JSON Schema for the TableRow component.
---

A single table row containing cells.

```json
{
  "type": "object",
  "properties": {
    "type": {
      "const": "TableRow",
      "default": "TableRow",
      "type": "string"
    },
    "cssClass": {
      "type": [
        "string",
        "null"
      ],
      "default": null,
      "description": "CSS/Tailwind classes for styling. Accepts a Responsive() for breakpoint-aware classes."
    },
    "children": {
      "type": "array",
      "items": {
        "$ref": "Component"
      }
    },
    "let": {
      "additionalProperties": true,
      "type": [
        "object",
        "null"
      ],
      "default": null,
      "description": "Scoped bindings available to children. Values are template strings."
    }
  },
  "required": [
    "type"
  ]
}
```
