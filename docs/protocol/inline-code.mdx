---
title: InlineCode
description: JSON Schema for the InlineCode component.
---

Inline code snippet.

```json
{
  "type": "object",
  "properties": {
    "type": {
      "const": "InlineCode",
      "default": "InlineCode",
      "type": "string"
    },
    "cssClass": {
      "type": [
        "string",
        "null"
      ],
      "default": null,
      "description": "CSS/Tailwind classes for styling. Accepts a Responsive() for breakpoint-aware classes."
    },
    "content": {
      "description": "Text content",
      "type": "string"
    },
    "bold": {
      "type": [
        "boolean",
        "null"
      ],
      "default": null,
      "description": "Render text in bold"
    },
    "italic": {
      "type": [
        "boolean",
        "null"
      ],
      "default": null,
      "description": "Render text in italic"
    },
    "align": {
      "enum": [
        "left",
        "center",
        "right",
        "justify"
      ],
      "type": [
        "string",
        "null"
      ],
      "default": null
    }
  },
  "required": [
    "type",
    "content"
  ]
}
```
