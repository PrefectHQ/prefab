---
title: State
description: JSON Schema for the State component.
---

Provide scoped interpolation values to children.

```json
{
  "type": "object",
  "properties": {
    "type": {
      "const": "State",
      "default": "State",
      "type": "string"
    },
    "cssClass": {
      "type": [
        "string",
        "null"
      ],
      "default": null,
      "description": "CSS/Tailwind classes for styling. Accepts a Responsive() for breakpoint-aware classes."
    },
    "children": {
      "type": "array",
      "items": {
        "$ref": "Component"
      }
    },
    "state": {
      "additionalProperties": true,
      "description": "Local state values scoped to children",
      "type": "object"
    }
  },
  "required": [
    "type"
  ]
}
```
