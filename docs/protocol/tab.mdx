---
title: Tab
description: JSON Schema for the Tab component.
---

A single tab panel within a Tabs container.

```json
{
  "type": "object",
  "properties": {
    "type": {
      "const": "Tab",
      "default": "Tab",
      "type": "string"
    },
    "cssClass": {
      "type": [
        "string",
        "null"
      ],
      "default": null,
      "description": "CSS/Tailwind classes for styling. Accepts a Responsive() for breakpoint-aware classes."
    },
    "children": {
      "type": "array",
      "items": {
        "$ref": "Component"
      }
    },
    "let": {
      "additionalProperties": true,
      "type": [
        "object",
        "null"
      ],
      "default": null,
      "description": "Scoped bindings available to children. Values are template strings."
    },
    "title": {
      "description": "Tab trigger label",
      "type": "string"
    },
    "value": {
      "type": [
        "string",
        "null"
      ],
      "default": null,
      "description": "Unique value for this tab (defaults to title)"
    },
    "disabled": {
      "default": false,
      "description": "Disable this tab",
      "type": "boolean"
    }
  },
  "required": [
    "type",
    "title"
  ]
}
```
