---
title: TableFooter
description: JSON Schema for the TableFooter component.
---

Table footer section.

```json
{
  "type": "object",
  "properties": {
    "type": {
      "const": "TableFooter",
      "default": "TableFooter",
      "type": "string"
    },
    "cssClass": {
      "type": [
        "string",
        "null"
      ],
      "default": null,
      "description": "CSS/Tailwind classes for styling. Accepts a Responsive() for breakpoint-aware classes."
    },
    "children": {
      "type": "array",
      "items": {
        "$ref": "Component"
      }
    }
  },
  "required": [
    "type"
  ]
}
```
