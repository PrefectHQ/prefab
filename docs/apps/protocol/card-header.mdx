---
title: CardHeader
description: JSON Schema for the CardHeader component.
---

Card header section for title and description.

```json
{
  "type": "object",
  "properties": {
    "type": {
      "const": "CardHeader",
      "default": "CardHeader",
      "type": "string"
    },
    "cssClass": {
      "type": [
        "string",
        "null"
      ],
      "default": null,
      "description": "CSS/Tailwind classes for styling"
    },
    "visibleWhen": {
      "type": [
        "string",
        "null"
      ],
      "default": null,
      "description": "State key \u2014 component renders only when this state value is truthy"
    },
    "children": {
      "type": "array",
      "items": {
        "$ref": "Component"
      }
    }
  },
  "required": [
    "type"
  ]
}
```
