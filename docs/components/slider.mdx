---
title: Slider
description: Range input for selecting numeric values.
icon: sliders
---

import { ComponentPreview } from "/snippets/component-preview.mdx";

Sliders let users select a numeric value from a range by dragging a handle. The default range is 0–100.

## Basic Usage

<ComponentPreview json={{"view":{"type":"Slider","min":0,"max":100,"value":50.0,"name":"slider-1","disabled":false}}}>
<CodeGroup>
```python Python icon="python"
from prefab_ui.components import Slider

Slider(value=50)
```
```json Protocol icon="brackets-curly"
{
  "view": {
    "type": "Slider",
    "min": 0,
    "max": 100,
    "value": 50.0,
    "name": "slider-1",
    "disabled": false
  }
}
```
</CodeGroup>
</ComponentPreview>

## Displaying the Value

Give the slider a `name` to store its current value in state, then reference it with `{{ }}` interpolation. Drag the slider and the label updates instantly:

<ComponentPreview json={{"view":{"cssClass":"gap-2","type":"Column","children":[{"cssClass":"justify-between","type":"Row","children":[{"type":"Label","text":"Volume"},{"content":"{{ volume | number }}%","bold":true,"type":"Text"}]},{"type":"Slider","min":0,"max":100,"value":75.0,"name":"volume","disabled":false}]},"state":{"volume":75}}}>
<CodeGroup>
```python Python icon="python"
from prefab_ui.app import set_initial_state
from prefab_ui.components import (
    Column,
    Label,
    Row,
    Slider,
    Text,
)

set_initial_state(volume=75)

with Column(gap=2):
    with Row(css_class="justify-between"):
        Label("Volume")
        Text("{{ volume | number }}%", bold=True)
    Slider(value=75, name="volume")
```
```json Protocol icon="brackets-curly"
{
  "view": {
    "cssClass": "gap-2",
    "type": "Column",
    "children": [
      {
        "cssClass": "justify-between",
        "type": "Row",
        "children": [
          {"type": "Label", "text": "Volume"},
          {"content": "{{ volume | number }}%", "bold": true, "type": "Text"}
        ]
      },
      {
        "type": "Slider",
        "min": 0,
        "max": 100,
        "value": 75.0,
        "name": "volume",
        "disabled": false
      }
    ]
  },
  "state": {"volume": 75}
}
```
</CodeGroup>
</ComponentPreview>

The `| number` pipe strips trailing decimals so the display stays clean. This pattern works with any component that supports interpolation.

## Range and Step

Use `min`, `max`, and `step` to constrain the slider. Here the range is 0–5 with half-step increments:

<ComponentPreview json={{"view":{"cssClass":"gap-2","type":"Column","children":[{"cssClass":"justify-between","type":"Row","children":[{"type":"Label","text":"Rating"},{"content":"{{ rating }}","bold":true,"type":"Text"}]},{"type":"Slider","min":0.0,"max":5.0,"value":4.0,"step":0.5,"name":"rating","disabled":false}]},"state":{"rating":4}}}>
<CodeGroup>
```python Python icon="python"
from prefab_ui.app import set_initial_state
from prefab_ui.components import (
    Column,
    Label,
    Row,
    Slider,
    Text,
)

set_initial_state(rating=4)

with Column(gap=2):
    with Row(css_class="justify-between"):
        Label("Rating")
        Text("{{ rating }}", bold=True)
    Slider(value=4, name="rating", min=0, max=5, step=0.5)
```
```json Protocol icon="brackets-curly"
{
  "view": {
    "cssClass": "gap-2",
    "type": "Column",
    "children": [
      {
        "cssClass": "justify-between",
        "type": "Row",
        "children": [
          {"type": "Label", "text": "Rating"},
          {"content": "{{ rating }}", "bold": true, "type": "Text"}
        ]
      },
      {
        "type": "Slider",
        "min": 0.0,
        "max": 5.0,
        "value": 4.0,
        "step": 0.5,
        "name": "rating",
        "disabled": false
      }
    ]
  },
  "state": {"rating": 4}
}
```
</CodeGroup>
</ComponentPreview>

## With Actions

The `on_change` parameter runs actions when the slider value changes. `$event` is the current numeric value:

<ComponentPreview json={{"view":{"cssClass":"gap-4","type":"Column","children":[{"type":"Slider","min":0,"max":100,"value":50.0,"name":"slider-2","disabled":false,"onChange":{"action":"setState","key":"level","value":"{{ $event }}"}},{"cssClass":"text-muted-foreground","content":"{{ level | 'Drag the slider' }}","type":"Text"}]}}}>
<CodeGroup>
```python Python icon="python"
from prefab_ui.components import Column, Slider, Text
from prefab_ui.actions import SetState

with Column(gap=4):
    Slider(
        value=50,
        on_change=SetState("level", "{{ $event }}"),
    )
    Text(
        "{{ level | 'Drag the slider' }}",
        css_class="text-muted-foreground",
    )
```
```json Protocol icon="brackets-curly"
{
  "view": {
    "cssClass": "gap-4",
    "type": "Column",
    "children": [
      {
        "type": "Slider",
        "min": 0,
        "max": 100,
        "value": 50.0,
        "name": "slider-2",
        "disabled": false,
        "onChange": {"action": "setState", "key": "level", "value": "{{ $event }}"}
      },
      {
        "cssClass": "text-muted-foreground",
        "content": "{{ level | 'Drag the slider' }}",
        "type": "Text"
      }
    ]
  }
}
```
</CodeGroup>
</ComponentPreview>

<Tip>
For simply displaying the slider's current value, [auto-binding with `name`](#displaying-the-value) is usually easier. Use `on_change` when you need to run custom logic like triggering a tool call or updating multiple state keys.
</Tip>

## Disabled State

<ComponentPreview json={{"view":{"type":"Slider","min":0,"max":100,"value":30.0,"name":"slider-3","disabled":true}}}>
<CodeGroup>
```python Python icon="python"
from prefab_ui.components import Slider

Slider(value=30, disabled=True)
```
```json Protocol icon="brackets-curly"
{
  "view": {
    "type": "Slider",
    "min": 0,
    "max": 100,
    "value": 30.0,
    "name": "slider-3",
    "disabled": true
  }
}
```
</CodeGroup>
</ComponentPreview>

## Variants

Semantic variants color the filled track to communicate meaning at a glance — the same palette available on Progress:

<ComponentPreview json={{"view":{"cssClass":"gap-4","type":"Column","children":[{"cssClass":"gap-2","type":"Column","children":[{"type":"Label","text":"Default"},{"type":"Slider","min":0,"max":100,"value":60.0,"name":"slider-4","disabled":false}]},{"cssClass":"gap-2","type":"Column","children":[{"type":"Label","text":"Success"},{"type":"Slider","min":0,"max":100,"value":100.0,"name":"slider-5","disabled":false,"variant":"success"}]},{"cssClass":"gap-2","type":"Column","children":[{"type":"Label","text":"Warning"},{"type":"Slider","min":0,"max":100,"value":72.0,"name":"slider-6","disabled":false,"variant":"warning"}]},{"cssClass":"gap-2","type":"Column","children":[{"type":"Label","text":"Destructive"},{"type":"Slider","min":0,"max":100,"value":90.0,"name":"slider-7","disabled":false,"variant":"destructive"}]},{"cssClass":"gap-2","type":"Column","children":[{"type":"Label","text":"Info"},{"type":"Slider","min":0,"max":100,"value":45.0,"name":"slider-8","disabled":false,"variant":"info"}]},{"cssClass":"gap-2","type":"Column","children":[{"type":"Label","text":"Muted"},{"type":"Slider","min":0,"max":100,"value":30.0,"name":"slider-9","disabled":false,"variant":"muted"}]}]}}}>
<CodeGroup>
```python Python icon="python"
from prefab_ui.components import Column, Label, Slider

with Column(gap=4):
    with Column(gap=2):
        Label("Default")
        Slider(value=60)
    with Column(gap=2):
        Label("Success")
        Slider(value=100, variant="success")
    with Column(gap=2):
        Label("Warning")
        Slider(value=72, variant="warning")
    with Column(gap=2):
        Label("Destructive")
        Slider(value=90, variant="destructive")
    with Column(gap=2):
        Label("Info")
        Slider(value=45, variant="info")
    with Column(gap=2):
        Label("Muted")
        Slider(value=30, variant="muted")
```
```json Protocol icon="brackets-curly"
{
  "view": {
    "cssClass": "gap-4",
    "type": "Column",
    "children": [
      {
        "cssClass": "gap-2",
        "type": "Column",
        "children": [
          {"type": "Label", "text": "Default"},
          {
            "type": "Slider",
            "min": 0,
            "max": 100,
            "value": 60.0,
            "name": "slider-4",
            "disabled": false
          }
        ]
      },
      {
        "cssClass": "gap-2",
        "type": "Column",
        "children": [
          {"type": "Label", "text": "Success"},
          {
            "type": "Slider",
            "min": 0,
            "max": 100,
            "value": 100.0,
            "name": "slider-5",
            "disabled": false,
            "variant": "success"
          }
        ]
      },
      {
        "cssClass": "gap-2",
        "type": "Column",
        "children": [
          {"type": "Label", "text": "Warning"},
          {
            "type": "Slider",
            "min": 0,
            "max": 100,
            "value": 72.0,
            "name": "slider-6",
            "disabled": false,
            "variant": "warning"
          }
        ]
      },
      {
        "cssClass": "gap-2",
        "type": "Column",
        "children": [
          {"type": "Label", "text": "Destructive"},
          {
            "type": "Slider",
            "min": 0,
            "max": 100,
            "value": 90.0,
            "name": "slider-7",
            "disabled": false,
            "variant": "destructive"
          }
        ]
      },
      {
        "cssClass": "gap-2",
        "type": "Column",
        "children": [
          {"type": "Label", "text": "Info"},
          {
            "type": "Slider",
            "min": 0,
            "max": 100,
            "value": 45.0,
            "name": "slider-8",
            "disabled": false,
            "variant": "info"
          }
        ]
      },
      {
        "cssClass": "gap-2",
        "type": "Column",
        "children": [
          {"type": "Label", "text": "Muted"},
          {
            "type": "Slider",
            "min": 0,
            "max": 100,
            "value": 30.0,
            "name": "slider-9",
            "disabled": false,
            "variant": "muted"
          }
        ]
      }
    ]
  }
}
```
</CodeGroup>
</ComponentPreview>

## Custom Colors

Use `indicator_class` for arbitrary track colors beyond the built-in variants:

<ComponentPreview json={{"view":{"cssClass":"gap-4","type":"Column","children":[{"cssClass":"gap-2","type":"Column","children":[{"type":"Label","text":"Storage Used"},{"type":"Slider","min":0,"max":100,"value":85.0,"name":"slider-10","disabled":false,"indicatorClass":"bg-red-500"}]},{"cssClass":"gap-2","type":"Column","children":[{"type":"Label","text":"Upload Progress"},{"type":"Slider","min":0,"max":100,"value":45.0,"name":"slider-11","disabled":false,"indicatorClass":"bg-blue-500"}]}]}}}>
<CodeGroup>
```python Python icon="python"
from prefab_ui.components import Column, Label, Slider

with Column(gap=4):
    with Column(gap=2):
        Label("Storage Used")
        Slider(value=85, indicator_class="bg-red-500")
    with Column(gap=2):
        Label("Upload Progress")
        Slider(value=45, indicator_class="bg-blue-500")
```
```json Protocol icon="brackets-curly"
{
  "view": {
    "cssClass": "gap-4",
    "type": "Column",
    "children": [
      {
        "cssClass": "gap-2",
        "type": "Column",
        "children": [
          {"type": "Label", "text": "Storage Used"},
          {
            "type": "Slider",
            "min": 0,
            "max": 100,
            "value": 85.0,
            "name": "slider-10",
            "disabled": false,
            "indicatorClass": "bg-red-500"
          }
        ]
      },
      {
        "cssClass": "gap-2",
        "type": "Column",
        "children": [
          {"type": "Label", "text": "Upload Progress"},
          {
            "type": "Slider",
            "min": 0,
            "max": 100,
            "value": 45.0,
            "name": "slider-11",
            "disabled": false,
            "indicatorClass": "bg-blue-500"
          }
        ]
      }
    ]
  }
}
```
</CodeGroup>
</ComponentPreview>

## Vertical Orientation

Set `orientation="vertical"` for vertical sliders. They fill their container's height, so wrap them in a fixed-height parent:

<ComponentPreview json={{"view":{"cssClass":"gap-8 h-48 items-center justify-center","type":"Row","children":[{"type":"Slider","min":0,"max":100,"value":75.0,"name":"slider-12","disabled":false,"orientation":"vertical"},{"type":"Slider","min":0,"max":100,"value":50.0,"name":"slider-13","disabled":false,"variant":"success","orientation":"vertical"},{"type":"Slider","min":0,"max":100,"value":25.0,"name":"slider-14","disabled":false,"variant":"info","orientation":"vertical"}]}}}>
<CodeGroup>
```python Python icon="python"
from prefab_ui.components import Row, Slider

with Row(css_class="gap-8 h-48 items-center justify-center"):
    Slider(value=75, orientation="vertical")
    Slider(value=50, orientation="vertical", variant="success")
    Slider(value=25, orientation="vertical", variant="info")
```
```json Protocol icon="brackets-curly"
{
  "view": {
    "cssClass": "gap-8 h-48 items-center justify-center",
    "type": "Row",
    "children": [
      {
        "type": "Slider",
        "min": 0,
        "max": 100,
        "value": 75.0,
        "name": "slider-12",
        "disabled": false,
        "orientation": "vertical"
      },
      {
        "type": "Slider",
        "min": 0,
        "max": 100,
        "value": 50.0,
        "name": "slider-13",
        "disabled": false,
        "variant": "success",
        "orientation": "vertical"
      },
      {
        "type": "Slider",
        "min": 0,
        "max": 100,
        "value": 25.0,
        "name": "slider-14",
        "disabled": false,
        "variant": "info",
        "orientation": "vertical"
      }
    ]
  }
}
```
</CodeGroup>
</ComponentPreview>

## Handle Styles

The `handle_style` parameter changes the thumb shape. Use `"bar"` for a tall rounded rectangle instead of the default circle:

<ComponentPreview json={{"view":{"cssClass":"gap-4","type":"Column","children":[{"cssClass":"gap-2","type":"Column","children":[{"type":"Label","text":"Circle (default)"},{"type":"Slider","min":0,"max":100,"value":50.0,"name":"slider-15","disabled":false}]},{"cssClass":"gap-2","type":"Column","children":[{"type":"Label","text":"Bar"},{"type":"Slider","min":0,"max":100,"value":50.0,"name":"slider-16","disabled":false,"handleStyle":"bar"}]}]}}}>
<CodeGroup>
```python Python icon="python"
from prefab_ui.components import Column, Label, Slider

with Column(gap=4):
    with Column(gap=2):
        Label("Circle (default)")
        Slider(value=50)
    with Column(gap=2):
        Label("Bar")
        Slider(value=50, handle_style="bar")
```
```json Protocol icon="brackets-curly"
{
  "view": {
    "cssClass": "gap-4",
    "type": "Column",
    "children": [
      {
        "cssClass": "gap-2",
        "type": "Column",
        "children": [
          {"type": "Label", "text": "Circle (default)"},
          {
            "type": "Slider",
            "min": 0,
            "max": 100,
            "value": 50.0,
            "name": "slider-15",
            "disabled": false
          }
        ]
      },
      {
        "cssClass": "gap-2",
        "type": "Column",
        "children": [
          {"type": "Label", "text": "Bar"},
          {
            "type": "Slider",
            "min": 0,
            "max": 100,
            "value": 50.0,
            "name": "slider-16",
            "disabled": false,
            "handleStyle": "bar"
          }
        ]
      }
    ]
  }
}
```
</CodeGroup>
</ComponentPreview>

## API Reference

<Card icon="code" title="Slider Parameters">

<ParamField body="value" type="float | None" default="None">
  Initial value of the slider.
</ParamField>

<ParamField body="name" type="str | None" default="None">
  State key for the slider's current value. Set this to bind the slider to state for display or form submission.
</ParamField>

<ParamField body="min" type="float" default="0">
  Minimum value of the range.
</ParamField>

<ParamField body="max" type="float" default="100">
  Maximum value of the range.
</ParamField>

<ParamField body="step" type="float | None" default="None">
  Step increment. If not provided, the slider moves continuously.
</ParamField>

<ParamField body="disabled" type="bool" default="False">
  Whether the slider is non-interactive.
</ParamField>

<ParamField body="variant" type="str" default="default">
  Visual variant for the filled track: `"default"`, `"success"`, `"warning"`, `"destructive"`, `"info"`, `"muted"`.
</ParamField>

<ParamField body="indicator_class" type="str | None" default="None">
  Tailwind classes for the filled track. Overrides variant coloring.
</ParamField>

<ParamField body="orientation" type="str" default="horizontal">
  Layout direction: `"horizontal"` or `"vertical"`. Vertical sliders fill their container's height.
</ParamField>

<ParamField body="handle_style" type="str" default="circle">
  Thumb shape: `"circle"` (default round) or `"bar"` (tall rounded rectangle).
</ParamField>

<ParamField body="on_change" type="Action | list[Action] | None" default="None">
  Action(s) to execute when the value changes. `$event` is the current numeric value.
</ParamField>

<ParamField body="css_class" type="str | None" default="None">
  Additional Tailwind CSS classes appended to the component's built-in styles.
</ParamField>
</Card>

## Protocol Reference

```json Slider
{
  "type": "Slider",
  "min?": 0,
  "max?": 100,
  "value?": "number | Action[]",
  "step?": "number",
  "range?": false,
  "name?": "string",
  "disabled?": false,
  "variant?": "default | success | warning | destructive | info | muted",
  "indicatorClass?": "string",
  "orientation?": "horizontal | vertical",
  "handleStyle?": "circle | bar",
  "onChange?": "Action | Action[]",
  "cssClass?": "string"
}
```

For the complete protocol schema, see [Slider](/protocol/slider).
