---
title: Div & Span
description: Unstyled block and inline containers for custom layouts and inline text.
icon: square-dashed
---

import { ComponentPreview } from '/snippets/component-preview.mdx'

Div and Span are unstyled primitives — the escape hatches for when Row, Column, and Grid don't express what you need.

## Div

A block container with no default styling. Style it entirely through `css_class`:

<ComponentPreview auto json={`{"$protocol":"prefab","view":{"cssClass":"flex items-center gap-3 rounded-lg border p-4","type":"Div","children":[{"cssClass":"bg-emerald-500 rounded-md h-8 w-8","type":"Div"},{"content":"Custom layout with Div","type":"P"}]}}`}>
<CodeGroup>
```python Python icon="python"
from prefab_ui.components import (
    Div,
    P,
)

with Div(css_class="flex items-center gap-3 rounded-lg border p-4"):
    Div(css_class="bg-emerald-500 rounded-md h-8 w-8")
    P("Custom layout with Div")
```
```json Protocol icon="brackets-curly"
{
  "$protocol": "prefab",
  "view": {
    "cssClass": "flex items-center gap-3 rounded-lg border p-4",
    "type": "Div",
    "children": [
      {
        "cssClass": "bg-emerald-500 rounded-md h-8 w-8",
        "type": "Div"
      },
      {
        "content": "Custom layout with Div",
        "type": "P"
      }
    ]
  }
}
```
</CodeGroup>
</ComponentPreview>

## Span

An inline text element with no default styling. Useful for inline annotations or colored text:

<ComponentPreview auto json={`{"$protocol":"prefab","view":{"cssClass":"gap-2 items-baseline","type":"Row","children":[{"content":"Status:","type":"P"},{"cssClass":"text-sm text-success font-medium","type":"Span","content":"Online"}]}}`}>
<CodeGroup>
```python Python icon="python"
from prefab_ui.components import (
    P,
    Row,
    Span,
)

with Row(gap=2, align="baseline"):
    P("Status:")
    Span("Online", css_class="text-sm text-success font-medium")
```
```json Protocol icon="brackets-curly"
{
  "$protocol": "prefab",
  "view": {
    "cssClass": "gap-2 items-baseline",
    "type": "Row",
    "children": [
      {
        "content": "Status:",
        "type": "P"
      },
      {
        "cssClass": "text-sm text-success font-medium",
        "type": "Span",
        "content": "Online"
      }
    ]
  }
}
```
</CodeGroup>
</ComponentPreview>

## API Reference

<Card icon="code" title="Div Parameters">
<ParamField body="css_class" type="str | None" default="None">
  Tailwind CSS classes. Div has no built-in styling.
</ParamField>
</Card>

<Card icon="code" title="Span Parameters">
<ParamField body="content" type="str" required>
  Text content. Can be passed as a positional argument.
</ParamField>

<ParamField body="css_class" type="str | None" default="None">
  Tailwind CSS classes. Span has no built-in styling.
</ParamField>
</Card>

## Protocol Reference

For the complete protocol schemas, see [Div](/protocol/div) and [Span](/protocol/span).

```json Div
{
  "type": "Div",
  "cssClass?": "string",
  "visibleWhen?": "string",
  "children?": []
}
```

```json Span
{
  "type": "Span",
  "content": "string (required)",
  "cssClass?": "string",
  "visibleWhen?": "string"
}
```
