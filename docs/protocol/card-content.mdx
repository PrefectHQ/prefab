---
title: CardContent
description: JSON Schema for the CardContent component.
---

Card content section for the main body.

```json
{
  "type": "object",
  "properties": {
    "type": {
      "const": "CardContent",
      "default": "CardContent",
      "type": "string"
    },
    "cssClass": {
      "type": [
        "string",
        "null"
      ],
      "default": null,
      "description": "CSS/Tailwind classes for styling"
    },
    "children": {
      "type": "array",
      "items": {
        "$ref": "Component"
      }
    }
  },
  "required": [
    "type"
  ]
}
```
