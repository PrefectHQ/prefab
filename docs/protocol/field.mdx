---
title: Field
description: JSON Schema for the Field component.
---

Choice card container for form controls.

```json
{
  "type": "object",
  "properties": {
    "type": {
      "const": "Field",
      "default": "Field",
      "type": "string"
    },
    "cssClass": {
      "type": [
        "string",
        "null"
      ],
      "default": null,
      "description": "CSS/Tailwind classes for styling. Accepts a Responsive() for breakpoint-aware classes."
    },
    "children": {
      "type": "array",
      "items": {
        "$ref": "Component"
      }
    },
    "let": {
      "additionalProperties": true,
      "type": [
        "object",
        "null"
      ],
      "default": null,
      "description": "Scoped bindings available to children. Values are template strings."
    },
    "title": {
      "description": "Card heading text",
      "type": "string"
    },
    "description": {
      "type": [
        "string",
        "null"
      ],
      "default": null,
      "description": "Secondary text below the title"
    },
    "disabled": {
      "default": false,
      "description": "Whether the field is dimmed and non-interactive",
      "type": "boolean"
    }
  },
  "required": [
    "type",
    "title"
  ]
}
```
