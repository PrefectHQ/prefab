---
title: Dialog
description: JSON Schema for the Dialog component.
---

Modal dialog overlay.

```json
{
  "type": "object",
  "properties": {
    "type": {
      "const": "Dialog",
      "default": "Dialog",
      "type": "string"
    },
    "cssClass": {
      "type": [
        "string",
        "null"
      ],
      "default": null,
      "description": "CSS/Tailwind classes for styling. Accepts a Responsive() for breakpoint-aware classes."
    },
    "children": {
      "type": "array",
      "items": {
        "$ref": "Component"
      }
    },
    "let": {
      "additionalProperties": true,
      "type": [
        "object",
        "null"
      ],
      "default": null,
      "description": "Scoped bindings available to children. Values are template strings."
    },
    "title": {
      "type": [
        "string",
        "null"
      ],
      "default": null,
      "description": "Dialog header title"
    },
    "description": {
      "type": [
        "string",
        "null"
      ],
      "default": null,
      "description": "Dialog header description"
    }
  },
  "required": [
    "type"
  ]
}
```
