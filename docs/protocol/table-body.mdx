---
title: TableBody
description: JSON Schema for the TableBody component.
---

Table body section containing data rows.

```json
{
  "type": "object",
  "properties": {
    "type": {
      "const": "TableBody",
      "default": "TableBody",
      "type": "string"
    },
    "cssClass": {
      "type": [
        "string",
        "null"
      ],
      "default": null,
      "description": "CSS/Tailwind classes for styling"
    },
    "children": {
      "type": "array",
      "items": {
        "$ref": "Component"
      }
    }
  },
  "required": [
    "type"
  ]
}
```
