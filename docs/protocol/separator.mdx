---
title: Separator
description: JSON Schema for the Separator component.
---

Visual divider component.

```json
{
  "type": "object",
  "properties": {
    "type": {
      "const": "Separator",
      "default": "Separator",
      "type": "string"
    },
    "cssClass": {
      "type": [
        "string",
        "null"
      ],
      "default": null,
      "description": "CSS/Tailwind classes for styling. Accepts a Responsive() for breakpoint-aware classes."
    },
    "orientation": {
      "default": "horizontal",
      "description": "Separator orientation",
      "enum": [
        "horizontal",
        "vertical"
      ],
      "type": "string"
    },
    "spacing": {
      "type": [
        "integer",
        "null"
      ],
      "default": null,
      "description": "Space around the separator (Tailwind spacing scale)"
    }
  },
  "required": [
    "type"
  ]
}
```
