---
title: AlertDescription
description: JSON Schema for the AlertDescription component.
---

Alert description text.

```json
{
  "type": "object",
  "properties": {
    "type": {
      "const": "AlertDescription",
      "default": "AlertDescription",
      "type": "string"
    },
    "cssClass": {
      "type": [
        "string",
        "null"
      ],
      "default": null,
      "description": "CSS/Tailwind classes for styling"
    },
    "visibleWhen": {
      "type": [
        "string",
        "null"
      ],
      "default": null,
      "description": "State key \u2014 component renders only when this state value is truthy"
    },
    "content": {
      "description": "Description text",
      "type": "string"
    }
  },
  "required": [
    "type",
    "content"
  ]
}
```
