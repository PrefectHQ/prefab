---
title: TableCaption
description: JSON Schema for the TableCaption component.
---

Table caption text.

```json
{
  "type": "object",
  "properties": {
    "type": {
      "const": "TableCaption",
      "default": "TableCaption",
      "type": "string"
    },
    "id": {
      "type": [
        "string",
        "null"
      ],
      "default": null,
      "description": "HTML id attribute for CSS targeting. Applied to the outermost element."
    },
    "css_class": {
      "type": [
        "string",
        "null"
      ],
      "default": null,
      "description": "CSS/Tailwind classes for styling. Accepts a Responsive() for breakpoint-aware classes."
    },
    "on_mount": {
      "default": null,
      "description": "Action(s) to execute when this component mounts."
    },
    "content": {
      "description": "Caption text",
      "type": "string"
    }
  },
  "required": [
    "type",
    "content"
  ]
}
```
