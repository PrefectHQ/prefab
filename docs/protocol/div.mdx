---
title: Div
description: JSON Schema for the Div component.
---

A bare container with no default styling.

```json
{
  "type": "object",
  "properties": {
    "type": {
      "const": "Div",
      "default": "Div",
      "type": "string"
    },
    "cssClass": {
      "type": [
        "string",
        "null"
      ],
      "default": null,
      "description": "CSS/Tailwind classes for styling"
    },
    "children": {
      "type": "array",
      "items": {
        "$ref": "Component"
      }
    }
  },
  "required": [
    "type"
  ]
}
```
