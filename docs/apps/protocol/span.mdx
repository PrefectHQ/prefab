---
title: Span
description: JSON Schema for the Span component.
---

An inline text element with no default styling.

```json
{
  "type": "object",
  "properties": {
    "type": {
      "const": "Span",
      "default": "Span",
      "type": "string"
    },
    "cssClass": {
      "type": [
        "string",
        "null"
      ],
      "default": null,
      "description": "CSS/Tailwind classes for styling"
    },
    "visibleWhen": {
      "type": [
        "string",
        "null"
      ],
      "default": null,
      "description": "State key \u2014 component renders only when this state value is truthy"
    },
    "content": {
      "description": "Text content with {{ field }} interpolation",
      "type": "string"
    }
  },
  "required": [
    "type",
    "content"
  ]
}
```
