---
title: Radio
description: JSON Schema for the Radio component.
---

Radio button input component.

```json
{
  "type": "object",
  "properties": {
    "type": {
      "const": "Radio",
      "default": "Radio",
      "type": "string"
    },
    "cssClass": {
      "type": [
        "string",
        "null"
      ],
      "default": null,
      "description": "CSS/Tailwind classes for styling. Accepts a Responsive() for breakpoint-aware classes."
    },
    "value": {
      "description": "Form value",
      "type": "string"
    },
    "label": {
      "type": [
        "string",
        "null"
      ],
      "default": null,
      "description": "Label text"
    },
    "checked": {
      "default": false,
      "description": "Whether radio is selected",
      "type": "boolean"
    },
    "name": {
      "type": [
        "string",
        "null"
      ],
      "default": null,
      "description": "Form field name"
    },
    "disabled": {
      "default": false,
      "description": "Whether radio is disabled",
      "type": "boolean"
    },
    "required": {
      "default": false,
      "description": "Whether radio is required",
      "type": "boolean"
    }
  },
  "required": [
    "type",
    "value"
  ]
}
```
