---
title: CardTitle
description: JSON Schema for the CardTitle component.
---

Card title text.

```json
{
  "type": "object",
  "properties": {
    "type": {
      "const": "CardTitle",
      "default": "CardTitle",
      "type": "string"
    },
    "cssClass": {
      "type": [
        "string",
        "null"
      ],
      "default": null,
      "description": "CSS/Tailwind classes for styling"
    },
    "visibleWhen": {
      "type": [
        "string",
        "null"
      ],
      "default": null,
      "description": "State key \u2014 component renders only when this state value is truthy"
    },
    "children": {
      "type": "array",
      "items": {
        "$ref": "Component"
      }
    },
    "content": {
      "type": [
        "string",
        "null"
      ],
      "default": null,
      "description": "Title text (alternative to children)"
    }
  },
  "required": [
    "type"
  ]
}
```
