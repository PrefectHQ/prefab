---
title: Spinner
description: JSON Schema for the Spinner component.
---

Animated loading spinner.

```json
{
  "type": "object",
  "properties": {
    "type": {
      "const": "Spinner",
      "default": "Spinner",
      "type": "string"
    },
    "cssClass": {
      "type": [
        "string",
        "null"
      ],
      "default": null,
      "description": "CSS/Tailwind classes for styling"
    },
    "size": {
      "default": "default",
      "description": "Spinner size variant",
      "enum": [
        "sm",
        "default",
        "lg"
      ],
      "type": "string"
    }
  },
  "required": [
    "type"
  ]
}
```
