---
title: Slider
description: Range input for selecting numeric values.
icon: sliders
---

import { ComponentPreview } from "/snippets/component-preview.mdx";

## Usage

Sliders let users select a numeric value from a range by dragging a handle.

<ComponentPreview auto json={`{"type":"Slider","min":0.0,"max":100.0,"value":50.0,"disabled":false}`}>
<CodeGroup>
```python Python
from prefab_ui.components import Slider

Slider(min=0, max=100, value=50)
```
```json Protocol
{
  "type": "Slider",
  "min": 0.0,
  "max": 100.0,
  "value": 50.0,
  "disabled": false
}
```
</CodeGroup>
</ComponentPreview>

## With Step Increment

<ComponentPreview auto json={`{"type":"Slider","min":0.0,"max":10.0,"value":5.0,"step":0.5,"disabled":false}`}>
<CodeGroup>
```python Python
from prefab_ui.components import Slider

Slider(min=0, max=10, step=0.5, value=5)
```
```json Protocol
{
  "type": "Slider",
  "min": 0.0,
  "max": 10.0,
  "value": 5.0,
  "step": 0.5,
  "disabled": false
}
```
</CodeGroup>
</ComponentPreview>

## Different Ranges

<ComponentPreview auto json={`{"type":"Column","gap":4,"children":[{"type":"Column","gap":2,"children":[{"type":"Label","text":"Volume (0-100)"},{"type":"Slider","min":0.0,"max":100.0,"value":75.0,"disabled":false}]},{"type":"Column","gap":2,"children":[{"type":"Label","text":"Temperature (0-10)"},{"type":"Slider","min":0.0,"max":10.0,"value":7.0,"disabled":false}]}]}`}>
<CodeGroup>
```python Python
from prefab_ui.components import (
    Column,
    Label,
    Slider,
)

with Column(gap=4):
    with Column(gap=2):
        Label("Volume (0-100)")
        Slider(min=0, max=100, value=75)

    with Column(gap=2):
        Label("Temperature (0-10)")
        Slider(min=0, max=10, value=7)
```
```json Protocol
{
  "type": "Column",
  "gap": 4,
  "children": [
    {
      "type": "Column",
      "gap": 2,
      "children": [
        {
          "type": "Label",
          "text": "Volume (0-100)"
        },
        {
          "type": "Slider",
          "min": 0.0,
          "max": 100.0,
          "value": 75.0,
          "disabled": false
        }
      ]
    },
    {
      "type": "Column",
      "gap": 2,
      "children": [
        {
          "type": "Label",
          "text": "Temperature (0-10)"
        },
        {
          "type": "Slider",
          "min": 0.0,
          "max": 10.0,
          "value": 7.0,
          "disabled": false
        }
      ]
    }
  ]
}
```
</CodeGroup>
</ComponentPreview>

## Displaying the Value

Give the slider a `name` to store its value in state, then reference it with `{{ }}` interpolation in other components. Add a format specifier to control how the value displays:

<ComponentPreview auto json={`{"_tree":{"type":"Column","gap":4,"children":[{"type":"Column","gap":2,"children":[{"cssClass":"justify-between","type":"Row","children":[{"type":"Label","text":"Volume"},{"type":"Text","content":"{{ volume | number }}%","bold":true}]},{"type":"Slider","min":0.0,"max":100.0,"value":75.0,"name":"volume","disabled":false}]},{"type":"Progress","value":"{{ volume }}","max":100.0}]},"_state":{"volume":75}}`}>
<CodeGroup>
```python Python
from prefab_ui.components import (
    Column,
    Label,
    Progress,
    Row,
    Slider,
    Text,
)

set_initial_state(volume=75)

with Column(gap=4):
    with Column(gap=2):
        with Row(css_class="justify-between"):
            Label("Volume")
            Text("{{ volume | number }}%", bold=True)
        Slider(name="volume", min=0, max=100, value=75)
    Progress(value="{{ volume }}", max=100)
```
```json Protocol
{
  "_tree": {
    "type": "Column",
    "gap": 4,
    "children": [
      {
        "type": "Column",
        "gap": 2,
        "children": [
          {
            "cssClass": "justify-between",
            "type": "Row",
            "children": [
              {
                "type": "Label",
                "text": "Volume"
              },
              {
                "type": "Text",
                "content": "{{ volume | number }}%",
                "bold": true
              }
            ]
          },
          {
            "type": "Slider",
            "min": 0.0,
            "max": 100.0,
            "value": 75.0,
            "name": "volume",
            "disabled": false
          }
        ]
      },
      {
        "type": "Progress",
        "value": "{{ volume }}",
        "max": 100.0
      }
    ]
  },
  "_state": {
    "volume": 75
  }
}
```
</CodeGroup>
</ComponentPreview>

The slider, label, and progress bar all stay in sync — drag the slider and the displayed value and progress bar update instantly. This pattern works with any component that supports interpolation.

## Disabled State

<ComponentPreview auto json={`{"type":"Slider","min":0.0,"max":100.0,"value":30.0,"disabled":true}`}>
<CodeGroup>
```python Python
from prefab_ui.components import Slider

Slider(min=0, max=100, value=30, disabled=True)
```
```json Protocol
{
  "type": "Slider",
  "min": 0.0,
  "max": 100.0,
  "value": 30.0,
  "disabled": true
}
```
</CodeGroup>
</ComponentPreview>

## API Reference

<Card icon="code" title="Slider Parameters">

<ParamField path="min" type="float" default="0">
  Minimum value of the range.
</ParamField>

<ParamField path="max" type="float" default="100">
  Maximum value of the range.
</ParamField>

<ParamField path="value" type="float | None">
  Initial value. Supports `{{ field }}` interpolation.
</ParamField>

<ParamField path="step" type="float | None">
  Step increment for the slider. If not provided, slider moves continuously.
</ParamField>

<ParamField path="name" type="str | None">
  Form field name for submission.
</ParamField>

<ParamField path="disabled" type="bool" default="False">
  Whether the slider is disabled.
</ParamField>

<ParamField path="css_class" type="str | None">
  Additional CSS classes to apply.
</ParamField>
</Card>

## Protocol Reference

<Card icon="braces" title="Slider Protocol">
<ParamField body="cssClass" type="string | null">
  CSS/Tailwind classes for styling
</ParamField>

<ParamField body="visibleWhen" type="string | null">
  State key — component renders only when this state value is truthy
</ParamField>

<ParamField body="type" type="Slider" default="Slider">
  Component discriminator.
</ParamField>

<ParamField body="min" type="number" default="0">
  Minimum value
</ParamField>

<ParamField body="max" type="number" default="100">
  Maximum value
</ParamField>

<ParamField body="value" type="number | null">
  Initial value (supports {{ field }} interpolation)
</ParamField>

<ParamField body="step" type="number | null">
  Step increment
</ParamField>

<ParamField body="name" type="string | null">
  Form field name
</ParamField>

<ParamField body="disabled" type="boolean" default="false">
  Whether slider is disabled
</ParamField>

<ParamField body="onChange" type="Action | Action[] | null">
  Action(s) to execute when value changes
</ParamField>

</Card>
