---
title: H3
description: JSON Schema for the H3 component.
---

Subsection heading (h3).

```json
{
  "type": "object",
  "properties": {
    "type": {
      "const": "H3",
      "default": "H3",
      "type": "string"
    },
    "cssClass": {
      "type": [
        "string",
        "null"
      ],
      "default": null,
      "description": "CSS/Tailwind classes for styling. Accepts a Responsive() for breakpoint-aware classes."
    },
    "content": {
      "description": "Text content",
      "type": "string"
    },
    "bold": {
      "type": [
        "boolean",
        "null"
      ],
      "default": null,
      "description": "Render text in bold"
    },
    "italic": {
      "type": [
        "boolean",
        "null"
      ],
      "default": null,
      "description": "Render text in italic"
    },
    "align": {
      "enum": [
        "left",
        "center",
        "right",
        "justify"
      ],
      "type": [
        "string",
        "null"
      ],
      "default": null
    }
  },
  "required": [
    "type",
    "content"
  ]
}
```
